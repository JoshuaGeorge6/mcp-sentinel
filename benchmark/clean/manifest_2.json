{
  "server_name": "notes-app-mcp",
  "tools": [
    {
      "name": "save_note",
      "description": "Saves a note with the given text to the user's notebook and returns the note's unique ID.",
      "inputSchema": {
        "properties": {
          "text": {"type": "string", "description": "The text content of the note"}
        }
      }
    },
    {
      "name": "list_notes",
      "description": "Lists all notes currently saved in the user's notebook, sorted by creation date.",
      "inputSchema": {
        "properties": {}
      }
    },
    {
      "name": "search_notes",
      "description": "Searches saved notes for a given keyword and returns matching note IDs and titles.",
      "inputSchema": {
        "properties": {
          "query": {"type": "string", "description": "Keyword to search for"}
        }
      }
    }
  ]
}
